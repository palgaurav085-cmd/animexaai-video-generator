export const metadata = {
  title: "Animexa AI",
  description: "AI Video Generator",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
